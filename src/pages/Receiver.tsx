import { PlaceholderPage } from './PlaceholderPage'

export function ReceiverPage() {
  return <PlaceholderPage title="Receiver" description="Receiver protocol, channel map and live channel values." />
}
